const dbConfig = require('./db');

// Aquí puedes agregar más configuraciones, como por ejemplo:
// const mqttConfig = require('./mqtt');
// const telegramConfig = require('./telegram');

module.exports = {
  dbConfig,
  // mqttConfig,
  // telegramConfig,
  // Exporta otras configuraciones según sea necesario
};
